<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="startislands.team3.softwareenginerringteam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectPaymentActivity" />
        <activity android:name=".CashPaymentActivity" />
        <activity android:name=".CardPaymentActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".TransactionDetails" />
        <activity android:name=".CheckCheck" />
        <activity android:name=".PaymentListInfoActivity" />
        <activity android:name=".PaymentDetailInfoActivity"></activity>
        <activity android:name=".CheckCheck" />
        <activity android:name=".SaleActivity"></activity>
    </application>

</manifest>